# 小白安装说明（跟着做就行，全程不需要懂代码）

## 开始前，你只需要知道 3 件事

1. **这是给 Mac 用的**。iPhone / iPad 装不了（它们只负责接收上课提醒）。
2. **安装要联网**，会下载约 150MB 的浏览器组件，一般 2–5 分钟。
3. **第一次要授权一次日历权限**，做完以后每周都能自动用。

---

## 第 1 步：解压

双击 **`课表同步-Mac安装包.zip`**，会多出一个文件夹 **`课表同步-Mac安装包`**，打开它。

> 如果要给别人：把这个 zip 通过 AirDrop / 微信 / QQ 发过去，他按同样的步骤做即可。

---

## 第 2 步：双击安装

在文件夹里双击 **`双击安装.command`**。

会弹出一个黑色窗口，里面不断刷字，这是正常的，**一直等到出现「安装完成！」四个字**才算好。

⚠️ 如果弹窗提示「无法打开，因为它来自身份不明的开发者」：

> 在 **`双击安装.command`** 上点**右键 → 打开 → 再点「打开」**（只有第一次需要这样）。

---

## 第 3 步：双击启动

双击 **`双击启动.command`** → 浏览器会自动打开控制页 → 输入学校账号密码 → 点 **「同步」**。

看到右下角弹出「已写入『课表』日历（事件数 XX）」就成功了。
（关掉那个黑色窗口就是停止服务，要用的时候再双击启动即可。）

## 第 3.5 步（可选）：做个 Dock 图标，以后一键打开

双击 **`创建快捷方式.command`** → 会生成一个 **`课表同步.app`**，并自动放进「个人应用程序文件夹」。

然后把 **`课表同步.app` 拖到屏幕下方的 Dock** 上，以后点一下图标就直接用，不用再进文件夹。

> 双击这个 App **不会弹出黑色终端窗口**，约 1 秒后浏览器会自动打开控制页。
> 如果你看到了黑色窗口，说明点的是 `.command` 文件，改用 Dock 里的 App 即可。
>
> 用法：随时点 Dock 图标都会重新打开控制页（关掉浏览器页面也没关系，再点一次就行），
> 服务不会重复开启。**想彻底停掉后台服务**就双击文件夹里的 `停止服务.command`。

---

## 第 4 步：第一次还要做一次授权（很重要，只做一次）

1. 打开 Mac 自带的 **「终端」** App（启动台里搜「终端」）
2. 输入 `cd` 加一个**空格**，然后把刚才那个**文件夹直接拖进终端窗口**，路径会自动填上，按回车
3. 粘贴下面这行并按回车：

```bash
.venv/bin/python main.py --run
```

4. 会弹出“想要访问日历”的窗口，**点「允许」**

没做这一步的话，以后自动同步会提示「权限违例 -10004」。

---

## 让手表真的响起来

1. Mac：**系统设置 → 日历 → 默认日历账户** 改成 **iCloud**（最关键）
2. iPhone：**设置 → 日历 → 账户 → iCloud → 打开「日历」开关**
3. Apple Watch：iPhone 上的 **Watch App → 日历 → 勾选「课表」**，表盘再加个「日历」小组件

---

## 常见问题

| 情况 | 怎么办 |
|------|--------|
| 双击没反应 / 弹「无法打开」 | 右键该文件 → 打开 → 打开 |
| 安装中途报错、断网 | 联网后重新双击「双击安装.command」，可以接着装 |
| 提示「多个名为课表的日历」 | 「日历」App 里把多余的「课表」删到只剩一个 |
| Mac 日历有课、手机没有 | 「课表」日历在「我的 Mac」分组了：见上面「让手表真的响起来」第 1 条，改完删掉旧日历重点「同步」 |
| 网页打不开 | 重新双击「双击启动.command」 |
| 浏览器显示 **403 / 拒绝访问** | 端口被苹果「隔空播放接收器」抢了。**本程序会自动换端口**（终端窗口里会写真实地址，浏览器也会自动打开新地址）；想一劳永逸：**系统设置 → 通用 → 隔空播放与接续 → 关闭「隔空播放接收器」** |

---

## 目录里的东西都是什么

| 文件名 | 你要不要动 |
|--------|-----------|
| `双击安装.command` | ✅ 双击它安装 |
| `双击启动.command` | ✅ 双击它使用 |
| `创建快捷方式.command` | ✅ 双击生成「课表同步.app」，可拖到 Dock |
| `停止服务.command` | 想彻底关掉后台服务时双击（平时用不到） |
| `README.md` | 想深入看（命令行、自动同步、卸载）就打开 |
| `install.sh` / `start.command` | 给会敲命令的人用的，忽略即可 |
| 其它 `.py / .sh / .plist` | 程序本身，不要改、不要删 |
